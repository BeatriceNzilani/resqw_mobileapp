name: resqw_mobileapp
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  video_thumbnail: ^0.5.6

  # --- CORE BACKEND ---
  supabase_flutter: ^2.12.2
  
  # --- MEDIA & FILES ---
  image_picker: ^1.0.7
  file_picker: ^8.0.0
  path_provider: ^2.1.2
  
  # --- MAPS & LOCATION ---
  geolocator: ^13.0.0
  geocoding: ^3.0.0
  
  # --- UTILS ---
  uuid: ^4.3.3
  google_maps_flutter: ^2.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true